{
  "name": "gamegamegame",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de jogos multiplayer GameGameGame.",
  "workspaces": [
    "apps/lobby",
    "apps/whoami",
    "apps/impostor",
    "apps/caracol",
    "packages/*"
  ],
  "scripts": {
    "build:games": "npm run build --workspaces --if-present",
    "test:games": "npm run test --workspaces --if-present",
    "install:lafarmer": "npm --prefix apps/lafarmer ci --ignore-scripts",
    "build:lafarmer": "npm run install:lafarmer && npm --prefix apps/lafarmer run build",
    "test:lafarmer": "npm run install:lafarmer && npm --prefix apps/lafarmer run test",
    "build": "npm run build:games && npm run build:lafarmer",
    "test": "npm run test:games && npm run test:lafarmer",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22 <23"
  }
}
