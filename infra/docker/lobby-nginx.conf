server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location = /healthz {
    default_type application/json;
    return 200 '{"ok":true,"service":"lobby"}';
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
